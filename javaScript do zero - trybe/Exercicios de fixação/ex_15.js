let num = 22;
let mensagem;

function ifElseExercise() {
  if (num % 2 === 0) {
    mensagem = num + " é par!";
    console.log(num + " é par!");
  } else {
    mensagem = num + " é ímpar!";
    console.log(num + " é ímpar!");
  }
}
